# Mal — kopier til .env og lim inn token DER (ikke her).
#   cp .env.example .env
# Rediger .env og sett ACCESS_TOKEN=... (JWT fra app.ainm.no).
# .env er gitignored; .env.example skal aldri inneholde ekte token.

ACCESS_TOKEN=

# Valgfritt — standard er https://api.ainm.no
# AINM_API_BASE=https://api.ainm.no
