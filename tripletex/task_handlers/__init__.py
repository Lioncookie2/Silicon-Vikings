"""
Optional explicit handlers per task category.

The default agent (agent.py) uses an LLM to emit a JSON call plan.
Add module-level functions here if you want deterministic shortcuts for known task IDs later.
"""
